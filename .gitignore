node_modules/
.next/
dist/
build/
.cache/
.env*
# Any env file, however it is named — a file called "…env.local" slipped past
# the rule above and reached a commit with a live service-role key in it.
*env.local
*.env
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
/.figma/design-context/

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Cloudflare / OpenNext build output
.open-next/
.wrangler/
cloudflare-env.d.ts

# Migration safety copies — contain image data, not source
/backups/
